using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Notes : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    // je dois amérliorer les focntions qui ont des paramètres avec les retourne.
    // Le physique avec les histoires de différences de PC Ave c le time delta time ou LateUpdate ou autres
    // refaire le quizz jusqu'à que je comprenne
    // quand est ce que je mets gameobject, transform 
    // Instantiate(enemies[randomIndex], spawnPos, enemies[randomIndex].gameObject.transform.rotation);
}
